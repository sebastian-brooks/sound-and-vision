<section class="hero is-small is-dark">
    <div class="hero-body">
        <div class="container">
            <h1 class="title">
                ARTISTS
            </h1>
        </div>
    </div>
</section>

<section class="section is-centered">
    <div class="container">
        <% if user_signed_in? %>
        <%= link_to "CREATE ARTIST PROFILE", new_artist_path, class: "button is-medium is-link" %>
            <hr>
        <% end %>
        <%= render partial: "shared/artists" %>
    </div>
</section>
<section class="section is-centered">
    <div class="container">
        <%= will_paginate @artists %>
    </div>
</section>
