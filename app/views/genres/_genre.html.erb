<%= form_with model: @genre, local: true do |f| %>
    <div>
        <%= f.label :name, class: "label" %>
        <%= f.text_field :name, class: "input" %><br /><br />
        <%= f.submit "Add Genre", class: "button is-link" %>
    </div>
<% end %>